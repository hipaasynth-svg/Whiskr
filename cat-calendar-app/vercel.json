{
  "version": 2,
  "builds": [{ "src": "server.js", "use": "@vercel/node", "config": { "includeFiles": ["content/**"] } }],
  "routes": [{ "src": "/(.*)", "dest": "/server.js" }],
  "crons": [{ "path": "/api/cron/daily", "schedule": "0 9 * * *" }]
}
